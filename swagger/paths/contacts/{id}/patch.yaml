tags:
  - Contacts
summary: Patch contact by id
operationId: patchContact
description: 'Patch a contact with payload'
security:
  - bearerAuth: []
parameters:
  - in: path
    name: id
    required: true
    schema:
      type: string
      example: '65ca67e7ae7f10c88b598384'
requestBody:
  required: true
  content:
    multipart/form-data:
      schema:
        type: object
        properties:
          name:
            description: "Contact's name"
            type: string
          phoneNumber:
            description: "Contact's phone number"
            type: string
          contactType:
            description: "Contact's contact type"
            type: string
            enum: ['work', 'home', 'personal']
          email:
            description: "Contact's email. Should be between 3 and 20"
            type: string
          isFavourite:
            description: "Whether is contact's favourite"
            type: boolean
          userId:
            description: "Contact's user id"
            type: string
          photo:
            description: "Link to contact's photo"
            type: string
            format: binary
responses:
  '201':
    description: Successfully upserted a contact
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
          properties:
            status:
              type: integer
              example: 201
            message:
              type: string
              example: Successfully upserted a contact
            data:
              type: object
              $ref: ../../../components/schemas/contact.yaml
  '401':
    $ref: '../../../components/responses/401.yaml'
  '404':
    $ref: '../../../components/responses/404.yaml'
  '500':
    $ref: '../../../components/responses/500.yaml'
